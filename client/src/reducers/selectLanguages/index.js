

export const initialState = {
  languageFrom: null,
  languageTo: null,
};

export default function selectLanguages(state = initialState, action) {
  switch (action.type) {

    default:
      return state;
  }
}
